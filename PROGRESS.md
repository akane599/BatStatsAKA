# BatStats Development Progress

## Baseline, scope and authorization
- Start: `76bc831328572c81717b97ffb0e280b10b14b8ad` (source6.2.6/code734). Branch: `codex/android16-reliability`. Initial untracked requested AGENTS.md incorporated; no unrelated changes.
- Supplied APK: `/tmp/batstats-artifacts/baseline-6.2.6.apk`, package `org.mlm.batstats`, name6.2.6/code735, target37. SHA256 `4009bc1f13d7c871ddc1c61f3696ed224f1ad5fbfe547ed452fc2aeaca86b3c5`; verified signerSHA256 `4aed2f691df64a7b0fea25a6b8c80183c6dc520e049dac0178defa1d6472228f`. Original key unavailable. Debug `.debug` coexists; prepare separately signed non-debug `.preview` too.
- Goal: accurate, efficient Android16/API36 monitoring, Shizuku primary; ordinary/ADB/Root modes, useful rich notification, modern accessible UI, reliable history/export/diagnostics, tests/APKs/workflow/PR description.
- Local work/dependencies/emulators/branches/commits authorized. **No GitHub writes, pushes or Actions approved/performed.** Prepare concrete publication scope only after local validation; existing push/PR workflows may auto-run.
- No physical Samsung available; do not request phone testing. Software emulator/synthetic tests cannot establish real current accuracy, capacity, energy savings or One UI lifecycle reliability.
- Follow AGENTS.md stage/commit/recovery rules. User-requested UX enhancements override generic audit skill scope restriction. Commit identity per-call: `git -c user.name=Codex -c user.email=codex@openai.com commit`; no global identity changes.

## Completed checkpoints
- `31c9a55`: persistent instructions and progress before implementation.
- `8ebc074`: APK/source baseline and prioritized confirmed defects (AUDIT_REPORT.md).
- `6e7e801`: API36 specialUse FGS, boot handling, stable Compose dependencies, Shizuku-first bounded command/framed-pipe transport, no backend fallback on failed reads.
- `08307d4`: validated nullable units, monotonic ObservationEngine and stable counter-based ETA.
- `e0d1eb3`: repository bounded single-owner actor/shared observation, auto sessions and DB4 nondestructive migrations/unique active session; shared notification/dashboard/drain periods; timestamp/gap-aware charts and responsive navigation; removed unsupported80/20mA per-app heuristic and unused duplicate hourly dumper. Existing detailed UID capabilities retained. Raw samples periodically trimmed to100k; representative charts~360 points.

## Actual validation
- Pristine metadata FAILED57s: alpha Compose1.13a03 requires SDK37.1; checked-in SDK37.0. Stable BOM2026.09.00/UI1.12.1 metadata passes with compile37/target36, version6.2.7-dev/code736.
- Stage3b debug APK assembled: `app/build/outputs/apk/debug/batstats-debug-6.2.7-dev-universal.apk` (+ABI splits). This is older than current source. Original API28/min26 charge-time lint error fixed and confirmed resolved in3d.
- Stage3c JVM + Android-test compilation PASSED4m40s,45 tests,0fail/error/skip (`/tmp/batstats-stage3c-tests.log`). Twelve new producer-based parser cases first reproduced12 failures before fixes. Android tests compiled, not executed.
- Stage3d JVM **51 tests passed,0fail/error/skip**. Combined tests/lint/assemble FAILED10m29s at lint with125 MissingTranslation errors,216 warnings; assembly after lint did not run. `/tmp/batstats-stage3d-build.log`. Complete real translations; no suppression or English placeholder copies.
- `python3 scripts/check_migrations.py` PASSED actualSQL1/2/3→4/schema/indices/row preservation/sentinels/active uniqueness, including rerun during stage4a. Android execution remains pending.
- Do not edit main/DAO sources during Kotlin/KSP compilation: earlier stale generated DAO adapter failure resolved by rebuild after source edits stopped.

## Stage3c checkpoint (`e95273c`)
- Corrected Android16 ADB gate: DUMP + PACKAGE_USAGE_STATS permission + allowed/default usage app-op; BATTERY_STATS alone is insufficient.
- Current non-consuming `dumpsys batterystats -c --charged` replaces `--checkin` (which may consume saved completed reports). Helper whitelist changed. Output bounded8MiB; parser discards included history.
- Reworked producer-backed v9 parser: nullable unsupported values, valid epoch/window, shared/multi-user UID maps, appUID classification, jobs/sync/background timers, alarm wakeup counts, f/p/w wakelocks, mobile-active µs→ms, global Bluetooth, UID activity/process/sensor/CPU frequencies, Android component estimates. Duplicate UID energy rows rejected; proportional total not added to base.
- Advanced screen rewritten with source/window/error visibility, complete scrolling lists and app search/details, separate current state, attribution/capacity limitations, resource strings. Removed cycle-derived health. Kernel collectors still need real privileged reads/units/errors (current UI is not final). Restore explicit confirmed system-stats reset control during next access/UI pass.
- Collector validates core epoch and stages results until access generation/source agree. Need finish stale access ViewModel, interruption/stop cancellation, diagnostics, failure publishing and reset concurrency.
- Standalone pure suite PASSED45 tests (16 parser cases +29 previous),1.07s JUnit runtime, `/tmp/batstats-parser-fixed-tests.log`. Full Gradle `:app:testDebugUnitTest :app:compileDebugAndroidTestKotlin` PASSED4m40s:45 tests,0fail/error/skip; Android test sources compile. `/tmp/batstats-stage3c-tests.log` (heap1024m/workers1).

## Environment and device validation
- JDK21, SDK35/36/37 +build-tools36, Gradle9.7.1; host3.8GiB RAM/4GiBswap, ~3.3GiB disk free at resume. Preserve unrelated processes/files/AVDs. An unrelated Java process exists; do not stop it.
- Task AVD `batstats_atd36`, port5580, software `-accel off`,1536MiB, no KVM. Own sparse6GiB userdata retained. Initial framework boot repeatedly hit watchdog; documented `ro.hw_timeout_multiplier=100` set on disposable ATD, framework restarted. **Boot completion1 verified before user pause.** No UI/screenshots/instrumentation success yet.
- Baseline install attempts: package service absent; then device still booting; third install interrupted at requested pause, result unknown. Verify installed packages after boot. Logs `/tmp/batstats-baseline-install*.log`.
- User paused; task emulator/processes stopped and work preserved. User resumed; recovery completed and emulator restarted (PID105403/session97691), log `/tmp/batstats-emulator-resumed.log`. Timeout100 restored; emulator subsequently stopped at host memory exhaustion. Keep emulator stopped during builds. Return adbd to shell before Shizuku shell-mode tests.
- Official Shizuku13.6.0 APK `/tmp/batstats-artifacts/shizuku-13.6.0.apk`; SHA256 `6e273ab0e991c4e79bc8b1bbb9b9dd739ccac1a8712a541a214078886b7b790f`, verified certificateSHA256 `268b5590e868fb08bae7e0ac413564cd1ff88f5ccff74af9dbd0dc918e30db30`. Not installed yet.
- Emulator battery100%,5000mV,25°C,900000µA are injected values, not physical measurements. Timeout accommodation is not app/device performance evidence.

## Remaining work / exact next action
1. Stage4b2 validated and ready for focused commit. Re-read this record, then implement4b3 bounded local diagnostics/source/freshness screen and deliberate sharing.
2. Stage4b: notification/collector failures, source/freshness diagnostics, settings/alerts, remaining efficiency/retention details. Stage4c: complete resource extraction/real translations and remaining UI/accessibility/history controls.
3. Device/API36/Shizuku/Binder integration and UI screenshots as emulator permits. Build debug+nondebug `.preview`, verify signatures/update guidance; prepare local manual Actions workflow/reports and PR description. Reconcile full audit coverage. No remote mutation without explicit approval.

## Stage3d checkpoint (`3584abc`)
- Shizuku helper v4 carries request IDs and caller-matched cancellation; bridge uses cancellable framed pipe worker and one-way Binder messages. Root/ADB commands use runInterruptible. Native dumpsys timeout/failure markers are rejected even after valid-looking partial records.
- Advanced ViewModel drops stale access state, uses shared collector and coalesces refresh; collector publishes only same-generation/source snapshots and serializes reset. Restored explicit system-reset confirmation, package-correct ADB commands and app sort controls.
- KernelStats pure parser + RootStatsCollector curated actual su reads: Linux ABI units, nullable unsupported fields, no cycle-health inference, CPU USER_HZ10ms ticks, modern wakeup_sources(ms) and legacy /proc/wakelocks(ns) normalized with explicit source. Bounded errors/timestamps in kernel UI.
- Standalone50 tests PASS1.166s (four kernel cases and cancellation added); newest dump-output test added afterward. Full Gradle JVM tests PASSED51 tests,0fail/error/skip. Combined tests/lint/assemble run FAILED10m29s at lint:125 MissingTranslation errors,216 warnings; APK assembly did not run. API28 guard now passes. `/tmp/batstats-stage3d-build.log`. Complete actual translations in stage4c; do not suppress checks or copy English as fake translations.
- Emulator boot recovery: native kernel traces show watchdog SysRq dumps before repeated zygote SIGKILL. `ro.hw_timeout_multiplier` was unset. On disposable ATD only, adb root succeeded and setprop ro.hw_timeout_multiplier100 verified. Requested zygote restart so framework reloads documented software-emulator timeout multiplier. This is an environment accommodation, not app/Samsung performance evidence; app test assertions unchanged. Need return adbd to shell (`adb unroot`) before Shizuku shell-mode validation. No app installed yet.
- Actual helper Binder cancellation remains unverified on device. Advanced notification must expose collector.error, not only shell.lastError; successful auxiliary commands must not hide failed batterystats. CPU/Doze empty labels and notification navigation remain pending. Collector coalescing timestamp reset/access-help path need correction.

## Stage4a checkpoint (`a2b9021`, history integrity)
- Bounded JSON/CSV history I/O (64MiB,100k samples,10k sessions); strict quoted CSV and JSON depth/string limits; nullable unit validation; stable imported identities and whole-file Room transactions. Reimports deduplicate; imported active snapshots close at last evidence. Existing local sessions remain authoritative; restored missing samples link to compatible native sessions without changing live totals. Conflicting observed points reject/roll back the import.
- Exports select overlapping sessions, retain full original session totals and record requested ranges, units, UTC timestamps and origins. Scrollable export screen uses live ending time and honors both selection toggles for JSON/CSV.
- HistoryMaintenance closes the start gate before waiting for imports; confirmed clear completes even on navigation cancellation. Service checks gate. Actor serialization discards queued samples; clear stops monitoring, deletes history, resets observation. Settings-count failure cannot pretend that deleted history remains. Clear dialog blocks duplicate taps and explains scope.
- Explicit cloud/device-transfer backup rules include only settings DataStore; history requires deliberate export. New strings are resourced, actual locale translations pending stage4c.
- Added JVM CSV/JSON bounds, identity/units/coverage and clear-race/cancellation tests; Android Room import/idempotence/rollback/range/file-roundtrip tests authored. JVM63 tests pass; eight Android import/query tests plus three migration tests compile, not executed. No Room schema change.
- Exact current validation command: `./gradlew :app:testDebugUnitTest :app:compileDebugAndroidTestKotlin --no-daemon -Dorg.gradle.jvmargs=-Xmx1024m --max-workers=1`.

Stage4a first run:12m total,63 JVM tests passed/0fail/error/skip; main source compiled; Android-test compilation failed on RoomDatabase `.use` receiver type (not a product failure, assertions unchanged). Emulator stopped at memory exhaustion (host3.7GiB used, swap4GiB full); property100 had been restored, boot still incomplete. Run builds and device testing sequentially from here. Own emulator105403 stopped; unrelated Java untouched. Follow-up source/tests must be revalidated.

Stage4a final validation: rerun session59049 PASSED2m6s (`/tmp/batstats-stage4a-retest.log`),63 JVM tests/0fail/error/skip, Android test compilation. Host `check_history_queries.py` initially needed optional empty schema indices handled, then passes actual SQL assertions; migration script and76 XML files pass. Dashboard chart now selects local BatteryManager source only. Periodic retention trims every200 inserts or24h to100k samples/10k sessions and retains active session even if older than imported records (up to200 new raw samples can accumulate before trim). Main/DAO edits after first build are included in the successful rerun.

Stage4b1 decisions: alert settings exist but no collection path calls Notifier alert functions. Implement alerts from validated ordinary readings only while monitoring, with persistent episode latches, hysteresis and repeated/time-qualified high-discharge readings; no wake locks/timers beyond existing sampling. Android channel controls sound/vibration after creation, so replace ineffective switches with a channel-settings action. Retain old settings fields as persisted compatibility data, remove their misleading visible controls.

## Stage4b1 checkpoint (`d628a90`, alerts and effective settings)
- Added pure BatteryAlerts engine: validated nullable values/power state, low/high/temperature hysteresis, Android FULL status (not percentage alone), repeated high discharge over3 readings/1min with gap/missing/sign checks; fixed IDs, saved episode latches across service restarts, coalesced full/high alerts. Service ignores samples predating its start; no extra timer/wakeup. Sound/vibration use Android notification settings; old preference values seed channel creation only.
- Removed ineffective visible controls for notification hiding/style, old foreground heuristic, compact view and export defaults; fields/keys remain persisted for compatibility. Rich foreground notification remains the useful default. Added sampling/retention guidance. Normal VIBRATE permission added for user-configured alert channel.
- Ten new JVM alert regressions now pass; final total73 tests and Android-test compilation pass. Both Gradle runs completed; no build currently running.
- Next:4b2 notification/diagnostics/widgets. Emulator remains stopped; run heavy builds and emulator sequentially.
- Additional confirmed UI localization issue: generated SettingMeta titleRes/descriptionRes/optionsRes are0 and RenderSettingField uses raw English metadata. Resource provider wrapper does not localize those raw titles. Correct all settings titles/options/descriptions via resources during4c, preserving current values/keys.

Stage4b1 initial validation PASSED2m7s (`/tmp/batstats-stage4b1-tests.log`),71 JVM tests and Android-test compilation. Follow-up: honor Android DISCHARGING status even with external power connected (insufficient supply); reject unknown status/negative plug flags; add connected-discharge and failed-delivery latch retry tests. Revalidation required before commit. Removed redundant Elvis warning; clarified high-current observations span1min, not continuous measurement.

Stage4b1 final validation PASSED1m46s (`/tmp/batstats-stage4b1-retest.log`),73 JVM tests/0fail/error/skip and Android-test compilation. Ten new alert tests cover missing values vs zero, hysteresis, charging/unplugging/full coalescing, persistent latches, high-current duration/gaps/vendor-sign uncertainty, connected discharge and retry after failed delivery. Generated schema confirms retired controls have meta=null while original persisted keys remain. New alert strings still need actual translations in4c; no device notification execution claimed.

Exact next stage4b2: fix collector coalescing timestamp/access-help path/root stale flag; preserve fractional mA; publish collector failures in rich notification; unify empty CPU/Doze reporting; correct notification destination and use a distinct PendingIntent identity (widget currently shares activity request0 and can overwrite notification extras); add bounded local diagnostic log/source screen and deliberate sharing; fix widget stopped/empty/freshness/temperature states. Review errors without logging raw per-app dumps. Heavy emulator work remains deferred until builds are finished.

## Stage4b2 checkpoint (notification and widgets)
- Collector coalescing/help/root stale state corrected; fractional current retained. Shared CPU/Doze text distinguishes missing intervals from observed zero. Rich notification includes collector failures, reading date and a distinct activity PendingIntent; initial/reused activity consumes the drain destination.
- Widgets request an ordinary snapshot under goAsync, show freshness/stopped state, hide ETA after stopping, honor temperature preference and use a valid common initial RemoteViews layout. No periodic widget alarm added.
- Gradle JVM tests and Android-test compilation PASSED2m19s (`/tmp/batstats-stage4b2-tests.log`):76 tests,0fail/error/skip. Three new engine-to-display regressions cover fictional screen-off consumption, reset/missing counters and empty CPU/Doze. Android notification construction/identity test compiles; device navigation/rendering remains unverified.
- Recovery checked full uncommitted diff and test results. Next stage4b3: bounded local diagnostics/source/freshness screen with deliberate sharing, then stage4c UI/localization. Emulator remains stopped during builds. No diagnostic store implemented yet.
