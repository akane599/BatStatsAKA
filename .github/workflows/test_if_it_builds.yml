name: CI
on: [push, pull_request]

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build-apk.yml
    with:
      run_android_tests: true
    # Pull requests use ephemeral development signing; no repository signing secrets.
